description: Success. Leaderboard returned.
content:
  application/json:
    schema:
      type: object
      properties:
        documentation:
          type: array
          items:
            type: object
            properties:
              organisationName:
                $ref: ../schemas/organisationName.yaml
              repositoryName:
                $ref: ../schemas/repositoryName.yaml
            additionalProperties: false
      additionalProperties: false
